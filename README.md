# Yandex Cloud S3 Poller

Демонстрационный проект для мониторинга изменений в S3 бакете Yandex Cloud и отправки уведомлений в Kafka.

## Описание

Скрипт периодически сканирует указанный S3 бакет, находит файлы, которые были изменены или созданы с момента последнего сканирования, генерирует presigned URL для каждого файла и отправляет информацию о них в Kafka топик.

## Возможности

- 🔄 Автоматический поллинг S3 бакета с настраиваемым интервалом
- 📅 Фильтрация файлов по дате изменения (только новые/обновленные файлы)
- 🔗 Генерация presigned URL для доступа к файлам
- 📨 Отправка уведомлений в Kafka топик
- 🌍 Использование локальной таймзоны машины для всех временных меток
- ✅ Полная типизация кода

## Требования

- Python >= 3.13
- Доступ к Yandex Cloud S3 бакету
- Kafka брокер (для отправки уведомлений)

## Установка

1. Клонируйте репозиторий или скачайте проект

2. Установите зависимости (рекомендуется использовать `uv`):

```bash
uv sync
```

Или с помощью `pip`:

```bash
pip install -e .
```

## Настройка

1. Создайте файл `.env` в корне проекта:

```env
key_id=YOUR_YANDEX_CLOUD_ACCESS_KEY_ID
key_secret=YOUR_YANDEX_CLOUD_SECRET_ACCESS_KEY
account_id=YOUR_YANDEX_CLOUD_ACCOUNT_ID
bucket_name=your-bucket-name
poll_interval=60
kafka_brokers=localhost:9092
kafka_topic=s3-files
```

### Параметры конфигурации

- `key_id` - Access Key ID для Yandex Cloud (обязательно)
- `key_secret` - Secret Access Key для Yandex Cloud (обязательно)
- `account_id` - ID аккаунта Yandex Cloud (обязательно)
- `bucket_name` - Имя S3 бакета для мониторинга (обязательно)
- `s3_endpoint` - URL endpoint для S3 (по умолчанию: `https://storage.yandexcloud.net`)
- `poll_interval` - Интервал поллинга в секундах (по умолчанию: 60, минимум: 1)
- `kafka_brokers` - Список Kafka брокеров через запятую (обязательно, например: `localhost:9092,localhost:9093`)
- `kafka_topic` - Имя Kafka топика для отправки уведомлений (обязательно)
- `log_level` - Уровень логирования (по умолчанию: `INFO`, возможные значения: `DEBUG`, `INFO`, `WARNING`, `ERROR`, `CRITICAL`)

## Использование

Запустите скрипт:

```bash
python main.py
```

Скрипт будет:

1. Подключаться к указанному S3 бакету
2. Сканировать бакет с заданным интервалом
3. Находить файлы, измененные с момента последнего сканирования
4. Генерировать presigned URL для каждого файла
5. Отправлять информацию о файлах в Kafka топик

Для остановки нажмите `Ctrl+C`.

## Формат сообщений в Kafka

Сообщения отправляются в формате JSON со следующей структурой:

```json
{
  "timestamp": "2026-01-15T17:16:29.169565+03:00",
  "files": [
    {
      "key": "path/to/file.txt",
      "last_modified": "2026-01-15T17:15:00.000000+03:00",
      "size": 1024,
      "etag": "abc123def456",
      "url": "https://storage.yandexcloud.net/bucket/path/to/file.txt?signature=..."
    }
  ],
  "count": 1
}
```

## Структура проекта

```
ya-cloud-s3-demo/
├── main.py              # Основной скрипт приложения
├── src/
│   ├── config.py        # Конфигурация (Pydantic Settings)
│   ├── models/          # Модели данных
│   │   ├── __init__.py
│   │   └── types.py     # Типы данных (TypedDict)
│   ├── s3/              # Модуль для работы с S3
│   │   ├── __init__.py
│   │   ├── client.py    # S3 клиент
│   │   └── scanner.py   # Сканер S3 бакета
│   ├── kafka/           # Модуль для работы с Kafka
│   │   ├── __init__.py
│   │   └── producer.py  # Kafka Producer клиент
│   ├── poller/          # Модуль поллера
│   │   ├── __init__.py
│   │   └── poller.py    # Основной класс поллера
│   └── utils/           # Утилиты
│       ├── __init__.py
│       ├── logging.py    # Настройка логирования
│       └── retry.py     # Декоратор для повторных попыток
├── .env                 # Переменные окружения (не коммитится)
├── pyproject.toml       # Зависимости проекта
└── README.md            # Документация
```

## Зависимости

- `boto3` - SDK для работы с AWS/Yandex Cloud S3
- `kafka-python-ng` - Клиент для Apache Kafka
- `pydantic` - Валидация данных и настроек
- `pydantic-settings` - Управление настройками из переменных окружения

## Особенности

- **Модульная архитектура**: Код разделен на логические модули (s3, kafka, poller, models, utils)
- **Локальная таймзона**: Все временные метки автоматически преобразуются в таймзону машины, на которой запускается скрипт
- **Инкрементальное сканирование**: Обрабатываются только файлы, измененные с момента последнего сканирования
- **Presigned URL**: Генерируются временные ссылки для доступа к файлам (срок действия: 1 час)
- **Обработка ошибок**: Улучшенная обработка ошибок с retry логикой для критических операций
- **Логирование**: Использование стандартного модуля `logging` вместо `print()`
- **Полная типизация**: Код полностью типизирован с использованием `typing` и `TypedDict`
- **Пагинация**: Используется пагинация для обработки больших бакетов
- **Валидация конфигурации**: Конфигурация валидируется с помощью Pydantic
- **Retry механизм**: Автоматические повторные попытки при ошибках S3 и Kafka

## Архитектура

### Основные компоненты

#### Модуль S3 (`src/s3/`)

- `S3Client` - Клиент для работы с S3 бакетом Yandex Cloud
  - Создание сессии и клиента Boto3
  - Генерация presigned URL для доступа к файлам
- `S3Scanner` - Сканер для поиска измененных файлов в S3 бакете
  - Сканирование бакета с пагинацией
  - Фильтрация файлов по дате изменения

#### Модуль Kafka (`src/kafka/`)

- `KafkaProducerClient` - Клиент для отправки сообщений в Kafka
  - Отправка списка файлов в Kafka топик
  - Сериализация сообщений в JSON

#### Модуль Poller (`src/poller/`)

- `S3Poller` - Основной класс поллера
  - Периодическое сканирование S3 бакета
  - Обработка найденных файлов
  - Отправка уведомлений в Kafka

#### Утилиты (`src/utils/`)

- `setup_logging()` - Настройка логирования
- `retry()` - Декоратор для повторных попыток выполнения операций

### Типы данных

Проект использует `TypedDict` для строгой типизации структур данных:

- `FileInfo` - Информация о файле (key, last_modified, size, etag, url)
- `KafkaMessage` - Структура сообщения для Kafka (timestamp, files, count)

## Примеры использования

### Базовый запуск

```bash
python main.py
```

### Запуск с логированием

```bash
python main.py 2>&1 | tee poller.log
```

### Изменение интервала поллинга

Отредактируйте `.env` файл:

```env
poll_interval=30  # Сканирование каждые 30 секунд
```

## Устранение неполадок

### Ошибка подключения к S3

- Проверьте правильность `key_id` и `key_secret` в `.env`
- Убедитесь, что у учетных данных есть доступ к указанному бакету
- Проверьте доступность endpoint `https://storage.yandexcloud.net`

### Ошибка подключения к Kafka

- Убедитесь, что Kafka брокер запущен и доступен
- Проверьте правильность адреса брокера в `kafka_brokers`
- Убедитесь, что топик `kafka_topic` существует (или создайте его)

### Файлы не находятся

- Проверьте, что файлы действительно были изменены после запуска скрипта
- Убедитесь, что имя бакета указано правильно
- Проверьте права доступа к бакету

## Разработка

### Требования для разработки

- Python >= 3.13
- `uv` (рекомендуется) или `pip`

### Установка зависимостей

```bash
uv sync
```

### Проверка типов

Проект использует полную типизацию. Для проверки типов можно использовать `mypy`:

```bash
pip install mypy
mypy main.py src/config.py
```

## Лицензия

Это демонстрационный проект для изучения работы с Yandex Cloud S3 и Kafka.
